import 'package:flutter/material.dart';

class Lists {
  Lists();
  EdgeInsets margin = const EdgeInsets.symmetric(vertical: 2, horizontal: 10);
  BorderRadius borderRadius = BorderRadius.circular(16);
  Color backgroundColor = const Color(0x47000000);
  TextStyle title = const TextStyle(color: Colors.white);
  TextStyle subtitle = const TextStyle(color: Colors.white);
  TextStyle description = const TextStyle(color: Colors.grey);
}
